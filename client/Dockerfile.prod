# Stage 1: Build the Next.js app
FROM node:18 AS builder

WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Copy application files and build the Next.js app
COPY . .
RUN npm run build

# Stage 2: Serve the app using a minimal Node.js environment
FROM node:18-alpine AS runner

WORKDIR /usr/src/app

# Copy the built files from the previous stage
COPY --from=builder /usr/src/app/.next ./.next
COPY --from=builder /usr/src/app/public ./public
COPY --from=builder /usr/src/app/node_modules ./node_modules
COPY --from=builder /usr/src/app/package.json ./package.json

# Expose port 3024 for the Next.js app
EXPOSE 3024

# Set the PORT environment variable to 3024
ENV PORT=3024

# Use the production start script
CMD ["npm", "run", "start"]
